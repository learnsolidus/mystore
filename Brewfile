# Redis - For ActionCable support (and Sidekiq, caching, etc)
brew "redis"

# PostgreSQL - brew install postgresql
brew "postgresql@16"

# Overmind (requires tmux)
brew "tmux"
brew "overmind"

# libvips - for processing images
brew "vips"
